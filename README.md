# پروژه سیستم نظرسنجی آنلاین ✅

## 🎯 هدف پروژه
پیاده‌سازی یک سیستم نظرسنجی آنلاین که کاربران بتوانند:
- ثبت‌نام و ورود کنند
- ایجاد نظرسنجی (برای کاربران مجاز)
- مشاهده نظرسنجی‌ها
- رأی‌دادن به گزینه‌ها
- مشاهده نتایج رأی‌گیری

---

## 🛠️ فناوری‌ها و ابزارها
| بخش | ابزار/کتابخانه |
|-----|----------------|
| Backend | Laravel +  |
| Database | MySQL (XAMPP) |
| Frontend | HTML + CSS |
| Package Manager | npm |
| Version Control | Git |

---

## 🧾 ساختار دیتابیس
ERD پروژه:
- USERS: ذخیره اطلاعات کاربران و مدیران
- SURVEYS: ذخیره هر نظرسنجی
- OPTIONS: گزینه‌های هر نظرسنجی
- VOTES: ثبت رأی کاربران یا مهمان‌ها

جداول و روابط کلیدی:
- هر کاربر می‌تواند چند نظرسنجی ایجاد کند (1 → N رابطه USERS → SURVEYS)  
- هر نظرسنجی چند گزینه دارد (1 → N رابطه SURVEYS → OPTIONS)  
- هر گزینه می‌تواند چند رأی دریافت کند (1 → N رابطه OPTIONS → VOTES)  

---

## 🧠 وضعیت فعلی پروژه (جلسه دوم)
- ✅ طراحی دیتابیس و ایجاد جدول‌ها در MySQL انجام شد
- ✅ ایجاد فایل SQL برای ساخت جداول
- ✅ راه‌اندازی Backend اولیه با Node.js و Express
- ✅ ایجاد یک Endpoint ساده (Hello World) برای تست سرور
- ✅ ساخت صفحه Frontend اولیه (HTML + CSS)
- ✅ ساخت README اولیه و توضیح اجرای پروژه

---

## 📁 ساختار پروژه
online-survey-project/
├─ backend/
│ ├─ server.js # فایل سرور Node.js
│ ├─ package.json # مدیریت پکیج‌ها
│ └─ node_modules/ # ماژول‌های نصب‌شده
├─ frontend/
│ ├─ index.html # صفحه اصلی
│ └─ style.css # استایل اولیه
├─ database.sql # اسکریپت ساخت دیتابیس
└─ README.md # این فایل



## 🚀 نصب و اجرای پروژه (برای مبتدی‌ها)

### 1. Clone پروژه
```bash
git clone <URL پروژه گیت‌هاب>
cd myblog


2. نصب بسته‌های Composer
composer install

3. تنظیم فایل محیطی

یک فایل .env بسازید (اگر موجود نیست، .env.example را کپی کنید):

cp .env.example .env


مقادیر زیر را مطابق MySQL خود تغییر دهید:

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=نام_دیتابیس
DB_USERNAME=نام_کاربری
DB_PASSWORD=رمز_عبور

4. ساخت کلید برنامه
php artisan key:generate

5. اجرای مهاجرت‌ها (جدول‌ها)
php artisan migrate

6. ساخت جدول Session
php artisan session:table
php artisan migrate

7. اجرای سرور داخلی لاراول
php artisan serve


🔐 امنیت و احراز هویت

رمز عبور‌ها با Hash ذخیره می‌شوند

احراز هویت با Session یا API Token

صفحات مدیریت فقط برای مدیران قابل دسترسی هستند

📊 ویژگی‌های آینده (اختیاری)

اعلان ایمیلی بعد از رأی‌دهی

اشتراک‌گذاری نظرسنجی‌ها در شبکه‌های اجتماعی

محدودیت زمانی برای نظرسنجی‌ها

خروجی PDF برای نتایج

💡 نکات

قبل از اجرای پروژه، مطمئن شوید XAMPP و MySQL فعال هستند

اگر ارور sessions table not found آمد، از php artisan session:table و php artisan migrate استفاده کنید



