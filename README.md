📊 سیستم نظرسنجی با Laravel

این پروژه یک سیستم کامل نظرسنجی تحت وب است که با فریم‌ورک Laravel پیاده‌سازی شده است. در این سیستم، مدیر می‌تواند نظرسنجی‌ها را ایجاد کند و کاربران (و حتی مهمان‌ها) می‌توانند در آن‌ها رأی بدهند. در عین حال، از رأی‌گیری تکراری جلوگیری می‌شود و نتایج هر نظرسنجی نیز قابل مشاهده است.

🎯 هدف پروژه

هدف این پروژه پیاده‌سازی یک سامانه نظرسنجی است که در آن:

امکان ساخت نظرسنجی توسط مدیر وجود دارد

کاربران و مهمان‌ها بتوانند در نظرسنجی شرکت کنند

هر کاربر یا IP فقط یک‌ بار در هر نظرسنجی رأی بدهد

نتایج هر نظرسنجی ذخیره و قابل نمایش باشد

صفحه عمومی برای نمایش تمام نظرسنجی‌ها وجود داشته باشد

✅ امکانات پیاده‌سازی‌شده تا این مرحله

ایجاد نظرسنجی توسط مدیر (شامل عنوان، سؤال و گزینه‌ها)

ذخیره نظرسنجی‌ها در دیتابیس

نمایش عمومی لیست نظرسنجی‌ها بدون نیاز به ورود

باز شدن صفحه اختصاصی برای هر نظرسنجی

امکان رأی دادن توسط کاربران لاگین‌شده و مهمان‌ها

جلوگیری از رأی تکراری با ترکیب IP و user_id

ایجاد جدول آرا در دیتابیس

شمارش و نمایش تعداد آرا برای هر گزینه

داشبورد کاربر برای مشاهده آمار نظرسنجی‌ها

🗂 ساختار دیتابیس پروژه

پروژه شامل سه جدول اصلی است:

1. جدول نظرسنجی‌ها

برای ذخیره اطلاعات اصلی هر نظرسنجی مانند عنوان، سؤال و وضعیت فعال یا غیرفعال.

2. جدول گزینه‌ها

برای ذخیره گزینه‌های هر نظرسنجی، به‌طوری که هر نظرسنجی می‌تواند چندین گزینه داشته باشد.

3. جدول آرا

برای ثبت هر رأی شامل شناسه نظرسنجی، گزینه انتخاب‌شده، IP کاربر و در صورت ورود، شناسه کاربر.

این ساختار باعث می‌شود کنترل رأی تکراری و محاسبه نتایج به‌سادگی انجام شود.

👨‍💻 نحوه عملکرد سیستم رأی‌دهی

زمانی که کاربر یا مهمان وارد صفحه یک نظرسنجی می‌شود:

لیست گزینه‌ها نمایش داده می‌شود

کاربر یکی از گزینه‌ها را انتخاب می‌کند

سیستم بررسی می‌کند که این کاربر یا IP قبلاً رأی داده است یا نه

اگر رأی تکراری باشد، اجازه ثبت داده نمی‌شود

اگر رأی جدید باشد، در دیتابیس ذخیره می‌شود

سپس پیام موفقیت نمایش داده می‌شود

🔒 جلوگیری از رأی تکراری

برای جلوگیری از تقلب و رأی دادن چندباره، سیستم به دو روش بررسی انجام می‌دهد:

اگر کاربر وارد حساب خود شده باشد → بررسی بر اساس شناسه کاربر (user_id)

اگر مهمان باشد → بررسی بر اساس آدرس IP

به این ترتیب، هر شخص فقط یک بار می‌تواند در هر نظرسنجی رأی بدهد.

📄 صفحات اصلی پروژه

صفحه عمومی نظرسنجی‌ها (برای همه کاربران و مهمان‌ها)

صفحه اختصاصی هر نظرسنجی برای رأی دادن

داشبورد کاربر (نمایش تعداد نظرسنجی‌ها و آرا)

پنل مدیر برای مدیریت نظرسنجی‌ها

🧪 تست دستی که انجام شده

ساخت یک نظرسنجی توسط مدیر

ثبت گزینه‌های مختلف برای آن

ورود به صفحه عمومی و مشاهده نظرسنجی

کلیک روی دکمه “رأی دادن”

ثبت یک رأی

تلاش برای رأی مجدد و مشاهده جلوگیری سیستم

مشاهده افزایش تعداد آرا برای گزینه انتخاب‌شده

تمامی تست‌ها با موفقیت انجام شده‌اند.

🚀 مراحل بعدی قابل انجام

در جلسات بعدی می‌توان امکانات زیر را اضافه کرد:

نمایش نمودار نتایج به‌صورت گرافیکی

بستن خودکار نظرسنجی بعد از زمان مشخص

تعیین زمان شروع و پایان برای هر نظرسنجی

امکان ویرایش و حذف نظرسنجی‌ها توسط مدیر

خروجی گرفتن از نتایج به صورت PDF یا Excel

افزودن سیستم نقش‌ها (Admin / User)

👤 مشخصات پروژه

طراحی و پیاده‌سازی شده توسط: Reza
زبان برنامه‌نویسی: PHP
فریم‌ورک: Laravel
نوع پروژه: سیستم نظرسنجی تحت وب
وضعیت: جلسه هفتم با موفقیت پیاده‌سازی شده ✅
